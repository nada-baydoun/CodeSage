# Copy this file to .env.local (for local dev) or set these as GitHub Actions secrets
# Only API routes require OPENAI_API_KEY at runtime.

# Required at runtime by API routes
OPENAI_API_KEY=

# Optional
TAVILY_API_KEY=

# Optional public data base URL (leave empty to use local public/data)
NEXT_PUBLIC_DATA_BASE_URL=
